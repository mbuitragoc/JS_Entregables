const fetch = require('node-fetch')

module.exports = {
gato: async function () {
       let dataApi = fetch('http://aws.random.cat/meow')
       .then(res => res.json())

       return dataApi.image;
   }
}